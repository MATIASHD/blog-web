mixin listaDeTags(tags)
    .d-flex.flex-wrap.gap-2
        if tags && tags.length
            each tag in tags
                include ./badge.pug
                +badge(tag)
        else
            span.text-secondary Sin tags
