mixin listaDeTags(tags, langPrefix)
    .d-flex.flex-wrap.gap-2
        if tags && tags.length
            each tag in tags
                a.badge.bg-secondary.text-decoration-none.px-3.py-2(href=`${langPrefix}/tags/${tag.slug}`)= tag.nombre
        else
            span.text-secondary Sin tags
