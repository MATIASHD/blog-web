mixin interactiveTable(headers, rows, idField)
    table.table 
        thread
            tr 
                th(scope="col")
                .form-check 
                    input.form-check-input(type="checkbox" id="selectAll")
                     
